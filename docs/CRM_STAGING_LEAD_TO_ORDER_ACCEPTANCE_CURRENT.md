# Текущий статус staging acceptance

Дата проверки: 31 июля 2026 года.

Сценарий `заявка → потребность → расчёт v1 → расчёт v2 → КП → заказ`
фактически прошёл в staging Supabase `otulfnouybahfnsycxqn`.

Подтверждены версии расчёта, суммы, себестоимость, скидка, дополнительный расход,
связь КП с v2, перенос двух позиций в заказ, идемпотентность и повторное чтение
заказа.

Сценарий завершён `ROLLBACK`. Остаток синтетических заявок, клиентов, расчётов,
КП и заказов равен нулю.

Итог:

`lead-to-order acceptance: OK; cleanup verified: zero residue`.

Production `ofewxuqfjhamgerwzull` не изменялся.
