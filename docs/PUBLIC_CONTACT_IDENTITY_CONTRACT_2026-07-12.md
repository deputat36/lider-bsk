# Контракт опубликованных контактов — 2026-07-12

Контур: только публичный сайт РА «Лидер».

## Текущий технический источник истины

До отдельного подтверждения полного NAP в issue #236 сайт использует уже опубликованные значения:

- телефон-ссылка: `tel:+79802457471`;
- отображаемый телефон: `8 980 245-74-71`;
- email: `zakaz@lider-bsk.ru`.

Этот документ не подтверждает юридический статус, адрес, часы работы, карты, мессенджеры, зоны доставки или монтажа. Эти сведения остаются открытыми в #236 и не должны добавляться без подтверждения владельца.

## Постоянная проверка

`tools/check_public_contact_identity.py` проверяет:

- все `tel:`-ссылки в корневых публичных HTML — здесь требуется строгий формат `tel:+79802457471`;
- все `mailto:`-ссылки в корневых публичных HTML;
- поля `telephone` и `email` в JSON-LD;
- обязательные контактные маркеры в `kontakty.html` и `privacy.html`;
- телефон аварийного способа связи в `assets/public-lead-form.js`;
- базовые значения в `tools/structured_data_pages.json`;
- отсутствие неподтверждённых NAP-полей в публичной JSON-LD-разметке и structured-data source.

JSON-LD может содержать визуально форматированный, но эквивалентный номер, например `+7-980-245-74-71`. Для сравнения JSON-LD номер нормализуется до цифр; другой фактический номер остаётся ошибкой.

## Pending NAP guard

Пока issue #236 не подтверждена владельцем, contract запрещает добавлять в публичную JSON-LD-разметку и business source следующие поля:

- `streetAddress`;
- `postalCode`;
- `openingHours`;
- `openingHoursSpecification`;
- `geo`, `latitude`, `longitude`;
- `sameAs`;
- `hasMap`.

Разрешены уже опубликованные общие географические сведения: Борисоглебск, Борисоглебский городской округ и Воронежская область. Точный адрес, часы работы, координаты, карты и официальные профили будут добавлены только после подтверждения.

Workflow: `.github/workflows/public-contact-identity-check.yml`. При ошибке workflow сохраняет полный диагностический отчёт в artifact на 7 дней.

## Что запрещено

- незаметно менять телефон или email только на части страниц;
- добавлять новый номер в `tel:` без синхронизации остальных публичных источников;
- публиковать неподтверждённый адрес, расписание, координаты или ссылку на карту;
- считать этот технический contract заменой полноценной NAP-проверки.

## Границы

CRM UI, `nav_*`, Supabase schema, Edge Functions, RLS, Auth и production data этим контрактом не изменяются.
