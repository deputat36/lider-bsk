# Аудит публичных заявок РА «Лидер»

Дата: 2026-06-24.

## Зачем нужен аудит

Аудит публичных заявок нужен для контроля связки:

`сайт → Edge Function → leader_leads → CRM`

Он помогает понять:

- была ли отправка формы;
- почему заявка не попала в CRM;
- не сработал ли honeypot;
- были ли пустые или подозрительные отправки;
- с какой страницы пришла заявка;
- какой `request_id` использовался;
- были ли ошибки записи.

## Таблица

Используется таблица:

`public.leader_public_lead_audit`

Основные поля:

- `created_at` — время события;
- `request_id` — идентификатор отправки;
- `phone_normalized` — телефон в нормализованном виде;
- `source_page_path` — путь страницы отправки;
- `page_url` — полный URL страницы;
- `user_agent` — браузер/устройство;
- `referer` — источник перехода;
- `utm_source`, `utm_medium`, `utm_campaign` — UTM-метки;
- `result` — результат обработки;
- `reason` — причина;
- `payload` — технические данные отправки.

## Результаты обработки

Возможные значения `result`:

- `accepted` — отправка принята;
- `duplicate` — дубль, если будет добавлена отдельная фиксация дублей;
- `suspicious` — подозрительная отправка, например заполнен honeypot;
- `rejected` — отклонено, например нет телефона и сообщения;
- `error` — ошибка записи заявки.

## RLS, GRANT и доступ

RLS включён.

Табличные права после ужатия 2026-06-23:

- `anon` — только `INSERT`;
- `authenticated` — только `SELECT`;
- `service_role` — служебный доступ сохранён.

Запись:

- разрешена публичному `anon`-контуру, чтобы `leader-public-lead` могла писать аудит через REST;
- insert-политика не использует открытый `WITH CHECK true`;
- публичная вставка ограничена ожидаемой формой события: обязательный `request_id`, допустимый `result`, JSON-object в `payload`, ограничение размера `payload`, временное окно `created_at`, лимиты длины технических полей.

Чтение:

- доступно только авторизованным активным сотрудникам из `leader_user_profiles` с ролями `owner`, `admin`, `manager`.

Важно:

- `anon` не должен иметь `SELECT`, `UPDATE`, `DELETE`, `TRUNCATE`, `REFERENCES`, `TRIGGER`;
- `authenticated` не должен иметь `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `REFERENCES`, `TRIGGER`.

## Edge Function

Функция:

`leader-public-lead`

Текущая версия:

`v7`

Что делает:

1. Принимает отправку с сайта.
2. Генерирует или принимает `request_id`.
3. Нормализует телефон.
4. Проверяет honeypot.
5. Проверяет наличие телефона или сообщения.
6. Пишет заявку в `leader_leads` с защитой от дублей по `request_id`.
7. Пишет событие в `leader_public_lead_audit`.
8. Проверяет HTTP-результат записи аудита.
9. При отказе REST/RLS пишет безопасную ошибку в журнал Edge Function без телефона и содержимого формы.

Важно:

- общий клиентский обработчик передаёт фактическое значение honeypot в Edge Function;
- заполненный honeypot больше не останавливается в браузере и фиксируется как `suspicious` с причиной `honeypot_filled`;
- подозрительная отправка не создаёт строку в `leader_leads`;
- ошибка записи аудита не блокирует получение основной заявки;
- аудит нужен для диагностики, а не для остановки продаж;
- диагностические маркеры ошибок: `leader_public_lead_audit_insert_failed` и `leader_public_lead_audit_request_failed`.

## CRM

Модуль:

`assets/v4/public-lead-audit-v1.js`

Подключение:

`assets/v4/site-cache-note-v1.js`

Раздел в меню:

`Аудит заявок`

Показывает последние 80 событий, счётчики, фильтры, дату, причину, телефон, `request_id`, страницу, UTM, user-agent, referer и технические данные.

## Текущее состояние данных

На момент проверки 2026-06-24 таблица аудита оставалась пустой. Это означает, что после развёртывания аудита ещё не было подтверждённой реальной отправки через текущую форму; само по себе это не подтверждает ошибку записи.

`rollback-smoke-test` вставки под ролью `anon` выполнен внутри транзакции и прошёл успешно, постоянная тестовая запись не создавалась.

## Как проверить

1. Открыть сайт и выполнить Ctrl + F5, чтобы загрузить текущий общий обработчик формы.
2. Отправить тестовую заявку с понятной пометкой.
3. Открыть CRM v4 и выполнить Ctrl + F5.
4. Открыть `Аудит заявок`.
5. Нажать `Обновить аудит`.
6. Проверить запись `Принято` и совпадение `request_id` с заявкой.
7. Если события нет, проверить журналы `leader-public-lead` на диагностические маркеры аудита.

Техническая проверка honeypot выполняется отдельно в инструментах разработчика или автоматизированным тестом: заполненное скрытое поле `website` должно создать только событие `suspicious`, без новой заявки.

## Что считать проблемой

- заявка появилась в CRM, но нет события аудита;
- событие аудита есть, а заявки нет, кроме ожидаемого события `suspicious`;
- honeypot создаёт реальную заявку;
- все события имеют статус `error`;
- в Edge Function появляются диагностические маркеры отказа аудита;
- раздел `Аудит заявок` не появляется после Ctrl + F5;
- сотрудник с ролью `owner/admin/manager` не может прочитать аудит;
- публичная форма начала возвращать ошибку после внедрения аудита;
- `anon` имеет любые права на аудит, кроме `INSERT`;
- `authenticated` имеет любые права на аудит, кроме `SELECT`.

## Где лежит код

Миграции:

- `supabase/migrations/20260621_leader_public_lead_audit.sql`;
- `supabase/migrations/20260622_tighten_public_lead_audit_insert_policy.sql`;
- `supabase/migrations/20260623_tighten_leader_public_lead_audit_grants.sql`.

Edge Function:

`supabase/functions/leader-public-lead/index.ts`

CRM-модуль:

`crm/v4/assets/v4/public-lead-audit-v1.js`
