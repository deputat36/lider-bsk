# Статус дизайн-направления РА «Лидер» — 2026-06-28

Дизайн теперь выделен в отдельное направление проекта.

Основной документ: `docs/DESIGN_ROADMAP_2026-06-28.md`.

## Что уже зафиксировано

- дизайн не сводится только к Open Graph или SEO;
- нужен единый фирменный чёрно-оранжевый стиль;
- публичные страницы услуг должны стать визуально едиными;
- форма заявки должна быть понятной до отправки и после отправки;
- CRM нужно улучшать аккуратно: интерфейс, пустые состояния, статусы и визуальная иерархия без изменения бизнес-логики.

## Что уже сделано в интерфейсе

- `request.html` стала понятнее и получила блоки `Что будет дальше`, `Перед отправкой`, `После отправки`.
- Добавлен блок `Выберите похожую задачу` с быстрыми сценариями.
- В CRM вкладка доступа переименована в `Доступ и роли`.
- Добавлена кнопка `Открыть доступ CRM`.
- Open Graph / Twitter Card добавлены на первые 8 страниц услуг.

## Что нужно сделать дальше

1. Создать `docs/DESIGN_SYSTEM_2026-06-28.md`.
2. Зафиксировать палитру, типографику, кнопки, карточки, CTA, FAQ и сетку.
3. Выбрать первый пакет страниц для визуальной унификации.
4. Улучшить визуальное состояние блока `Номер обращения` на странице заявки.
5. Добавить критерии ручной проверки дизайна на мобильной версии.

## Что требует согласования владельца

- Стиль: строгий чёрно-оранжевый, премиальный или более массовый городской.
- Использование текущего логотипа без изменений или подготовка нового UI-набора вокруг него.
- Нужны ли реальные фото Борисоглебска, производства и готовых работ.
- Можно ли менять структуру посадочных страниц или только оформление.
- Какие страницы услуг первыми переводить на новый дизайн-шаблон.

## Правило безопасности

Дизайн-задачи по умолчанию GitHub-only:

- Supabase production не менять;
- Edge Functions не деплоить;
- RLS, grants, таблицы и данные не трогать;
- бизнес-логику формы заявки не менять без отдельного approval.
