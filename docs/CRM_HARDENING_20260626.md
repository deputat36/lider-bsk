# CRM hardening — статус на 2026-06-26

Контур: CRM РА «Лидер», Supabase project `ofewxuqfjhamgerwzull`.

## Что уже применено в Supabase

### Pending-доступ новых пользователей

В базе создана модель приглашений:

- `leader_user_invites`;
- trigger активации профиля по приглашению;
- обновлённая логика `leader_ensure_profile(text)`;
- новые пользователи без приглашения получают неактивный профиль и ждут подтверждения владельцем или администратором.

Контрольная проверка после применения:

- активных профилей: 4;
- pending-профилей: 0;
- активных owner/admin: 3;
- приглашений: 0;
- политика вставки приглашений есть;
- trigger приглашений установлен.

### Атомарное создание заказа из КП

В базе создана RPC:

`leader_create_order_from_offer_rpc(jsonb)`

Назначение:

- принять `offer_id` согласованного КП;
- заблокировать КП, расчёт и заявку на время операции;
- проверить статус `Согласовано`;
- вернуть уже созданный заказ, если он уже связан с КП/расчётом/заявкой;
- создать или найти клиента;
- создать заказ;
- перенести позиции расчёта в позиции заказа;
- связать КП, расчёт и заявку с заказом;
- записать событие КП и историю статуса заказа.

Edge Function `leader-crm-leads` задеплоена в Supabase как version 10, `verify_jwt=true`, `ACTIVE`. Действие `create_order_from_offer` вызывает эту RPC через `service_role`.

После security advisor прямой execute `leader_create_order_from_offer_rpc(jsonb)` закрыт для `anon`, `authenticated`, `public`. В живой базе execute остался только у `postgres` и `service_role`.

## Что есть в GitHub

### PR #13 `crm-hardening-main-20260626`

Содержит актуальный набор CRM-изменений поверх свежего `main` на момент создания ветки:

- вкладка `Доступ` в меню CRM;
- разрешение вкладки `user_admin` в таб-роутере;
- компактный модуль `crm/v4/assets/v4/user-admin-v1.js`;
- обновлённый `auth.js` с pending-состоянием;
- Edge Function `supabase/functions/leader-crm-leads/index.ts`, соответствующая deployed version 10;
- исполняемые SQL-миграции pending-доступа;
- исполняемая SQL-миграция `leader_create_order_from_offer_rpc(jsonb)`;
- hardening-миграция `20260626_08_leader_order_rpc_restrict_execute.sql`;
- grants для profile functions;
- документация по текущему состоянию.

В ветке больше нет `.sql.todo`-заглушек.

### PR #11 `crm-hardening-20260626`

Старый PR содержит раннюю версию CRM-изменений и отстаёт от актуального `main` сильнее. Его не нужно мержить вслепую. После принятия PR #13 старый PR можно закрыть как заменённый.

## Почему ветка PR #13 пока draft

SQL и Edge Function уже синхронизированы с тем, что применено в Supabase. Оставшаяся причина держать PR draft — ветка разошлась с `main`.

На момент проверки:

- PR #13 ahead of `main`: 29 commits;
- PR #13 behind `main`: 5 commits;
- последние коммиты `main` относятся к Open Graph/публичным HTML-страницам и не пересекаются с CRM/Supabase-файлами PR #13.

## Что сделать перед финальным merge

1. Синхронизировать `crm-hardening-main-20260626` с последним `main`.
2. Дождаться повторного зелёного CI после синхронизации.
3. Проверить вручную:
   - вход активного пользователя;
   - pending-доступ нового пользователя;
   - вкладку `Доступ`;
   - создание и закрытие приглашения;
   - сценарий заявка → расчёт → КП `Согласовано` → заказ;
   - повторное создание заказа из того же КП без дубля.
4. После этого перевести PR #13 из draft и мержить.
5. Закрыть PR #11 как заменённый.

## Безопасное текущее состояние

Боевой Supabase-контур уже защищает новых пользователей и атомарно создаёт заказ из согласованного КП. Прямая RPC-конвертация заказа закрыта для обычных авторизованных пользователей и доступна только через Edge Function/service role. GitHub-ветка PR #13 теперь содержит реальные SQL-миграции и Edge Function, но должна быть синхронизирована с последним `main` перед merge.
