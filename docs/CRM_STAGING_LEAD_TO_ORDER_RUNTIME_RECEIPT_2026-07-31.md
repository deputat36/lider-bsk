# Runtime receipt: staging «заявка → заказ» — 2026-07-31

## Среда

- репозиторий: `deputat36/lider-bsk`;
- PR: `#472`;
- staging Supabase: `otulfnouybahfnsycxqn`;
- production Supabase: `ofewxuqfjhamgerwzull` — не изменялся.

## Выполненный сценарий

`заявка → потребность → расчёт v1 → расчёт v2 → КП → заказ`.

## Подтверждённые проверки

- ответственный и следующий контакт сохранены;
- потребность повторно прочитана;
- расчёт v1 не перезаписан;
- расчёт v2 получил номер 2;
- сумма v2: 2400;
- себестоимость v2: 1700;
- скидка и дополнительный расход сохранены в снимках позиций;
- КП создано из v2 и связано с ней;
- ответ RPC КП прочитан через фактический контракт `entity.id`;
- заказ создан с двумя позициями;
- сумма, себестоимость, ответственный и источник перенесены;
- повтор команды создания заказа не создал дубль;
- заказ успешно повторно прочитан.

## Очистка

Сценарий выполнен в транзакции с `ROLLBACK`.

Контрольный остаток после завершения:

- заявки: 0;
- клиенты: 0;
- расчёты: 0;
- КП: 0;
- заказы: 0.

Runtime result:

`lead-to-order acceptance: OK; cleanup verified: zero residue`.

## Безопасность

- catalog preflight: успешно;
- RLS новых staging-таблиц: включён;
- доступ `anon`: закрыт;
- доступ `authenticated`: закрыт;
- staging harness: только `service_role`;
- production cutover: не выполнялся;
- `nav_*` и `parket_*`: не затрагивались.
