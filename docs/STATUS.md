# Статус проекта РА «Лидер»

Дата обновления: 2026-06-21.

## Уже работает

### Публичный сайт

Сайт РА «Лидер» находится в основном репозитории `deputat36/lider-bsk`.

Проверено после аудита:

- `robots.txt` существует и оформлен построчно;
- `sitemap.xml` существует и имеет XML-структуру;
- заявки с сайта отправляются в Supabase Edge Function проекта CRM;
- публичная форма сайта передаёт `request_id`, `page_path`, `submitted_at` и технические данные источника;
- повторный клик по кнопке отправки блокируется на клиенте до завершения запроса.

Дополнительно исправлено 2026-06-21:

- восстановлено видимое отображение логотипа в шапке сайта через штатный знак и текст;
- добавлены CSS-ограничения от горизонтального переполнения страницы;
- улучшена адаптация шапки, навигации, сеток и блоков под экраны ноутбуков и планшетов.

### Supabase

Проверено 2026-06-21:

- Edge Function `leader-public-lead` активна, версия 6, публичная, без JWT для формы сайта;
- Edge Function `leader-crm-leads` активна, версия 7, JWT включён;
- Edge Function `leader-crm-orders` активна, версия 2, JWT включён;
- таблицы контура `leader_*` существуют и имеют включённый RLS;
- в контуре уже есть данные по заявкам, клиентам, заказам, позициям, расчётам, КП, производству и монтажу;
- таблица `leader_public_lead_audit` создана, RLS включён, строк пока 0 до следующей отправки формы;
- таблицы `nav_*` видны в той же базе, но к задачам РА «Лидер» не относятся и не используются.

### Публичные заявки

Выполнено усиление связки сайт → CRM:

- в `leader_leads` добавлены поля `request_id`, `phone_normalized`, `source_page_path`, `submitted_at`, `client_user_agent`;
- добавлена уникальность `request_id`;
- добавлен индекс для поиска дублей по `phone_normalized` и дате создания;
- Edge Function `leader-public-lead` обновлена до версии 6;
- серверная отправка заявки использует `on_conflict=request_id` и игнорирует повторную вставку дубля;
- добавлена таблица `leader_public_lead_audit` для аудита публичных отправок;
- добавлены индексы аудита по дате, `request_id` и нормализованному телефону;
- добавлена RLS-политика чтения аудита для активных сотрудников ролей owner/admin/manager;
- добавлена RLS-политика записи аудита через публичный anon-контур Edge Function;
- `leader-public-lead` пишет аудит для событий `accepted`, `suspicious`, `rejected`, `error`;
- аудит не блокирует получение заявки, если запись аудита не удалась.

### Документация

Созданы и добавлены в основной репозиторий:

- `AGENTS.md`;
- `docs/PROJECT.md`;
- `docs/STATUS.md`;
- `docs/DECISIONS.md`;
- `docs/AUDIT_IMPROVEMENTS.md`;
- `docs/LEADER_DB_HARDENING.md`;
- `docs/PUBLIC_LEAD_FORM_HARDENING.md`;
- `docs/STATIC_CHECKS_RUNBOOK.md`;
- `docs/CRM_V4_TRANSFER_FILES.md`;
- `docs/NEXT_SAFE_STEPS.md`;
- `docs/MANUAL_TEST_CHECKLIST.md`;
- `crm/v4/TRANSFER.md`;
- `supabase/migrations/20260621_leader_public_lead_audit.sql`;
- `supabase/functions/leader-public-lead/index.ts`.

### Автоматические проверки

Добавлен и усилен контур GitHub Actions:

- проверка `robots.txt`;
- проверка XML-структуры `sitemap.xml`;
- проверка, что URL из `sitemap.xml` ведут на существующие файлы;
- проверка наличия `assets/public-lead-form.css` и `assets/public-lead-form.js`;
- проверка, что форма сайта содержит `request_id`, `page_path`, `submitted_at`;
- проверка, что CSS формы содержит защиту от горизонтального переполнения;
- проверка, что CSS формы повторно не скрывает логотип сайта;
- базовый поиск случайно закоммиченных секретных ключей.

### CRM v4

CRM v4 временно размещена в `deputat36/lidercalculator`.

Рабочий путь CRM v4:

1. Вход.
2. Список заявок.
3. Карточка заявки.
4. Потребности клиента.
5. Сохранённые расчёты.
6. Коммерческие предложения.
7. Создание заказа из согласованного КП.
8. Контроль заказов, производства, монтажа и финансов.
9. Управленческий дашборд.

В рабочей версии CRM v4 обновлён модуль `assets/v4/offer-order-create-v1.js`: создание заказа из согласованного КП теперь вызывает серверное действие `create_order_from_offer`, а не собирает заказ и связи разрозненными шагами в браузере.

Дополнительно 2026-06-21 обновлена карточка заявки во временной CRM v4:

- добавлена краткая сводка в верхней части карточки;
- блоки подписаны по логике работы: контакт и статус → потребности → расчёты → КП;
- технические данные формы сворачиваются, чтобы не мешать менеджеру;
- кнопки и блок следующего контакта адаптированы под экран ноутбука.

Дополнительно 2026-06-21 обновлён список заявок:

- добавлена панель внимания по проблемным заявкам;
- отдельно считаются просроченные контакты;
- отдельно выделяются заявки без следующего шага;
- отдельно выделяются заявки без телефона;
- отдельно выделяются новые заявки старше суток.

Дополнительно 2026-06-21 подключён финансовый контроль:

- раздел `Финансы` добавляется в навигацию CRM v4 через штатный lazy-loader;
- показывает активную сумму заказов;
- показывает потенциальную прибыль и среднюю маржу;
- выделяет неоплаченные и частично оплаченные заказы;
- выделяет заказы без себестоимости;
- выделяет заказы с низкой маржей.

Дополнительно 2026-06-21 подключён управленческий дашборд:

- раздел `Дашборд` добавляется в навигацию CRM v4;
- рабочая версия дашборда загружается через `control-sections-lazy-loader-v1.js` из `management-dashboard-v3.js`;
- старый прямой wrapper `management-dashboard-v2.js` отключён, чтобы не было дубля разделов и конфликтов обработчиков;
- дашборд объединяет заявки, заказы, КП, производство, монтаж и финансы;
- показывает срочные риски;
- показывает новые заявки, просроченные контакты и заявки без телефона;
- показывает просроченные заказы и ближайшие сроки;
- показывает финансовые риски, открытые КП и просроченные КП.

Дополнительно 2026-06-21 выполнена очистка frontend-подключений CRM:

- из `offer-print.js` убраны прямые импорты `finance-control-v2.js` и `management-dashboard-v2.js`;
- `Финансы` и `Дашборд` теперь должны открываться через общий `control-sections-lazy-loader-v1.js`;
- добавлен файл `crm-ui-selfcheck-v1.js` для проверки, какие разделы CRM реально появились в меню;
- самопроверка подключается через `site-cache-note-v1.js`.

Дополнительно 2026-06-21 обновлены версии подключений в `app-v4.html`:

- `offer-print.js` поднят до версии `20260621-dashboard-1`;
- `lead-card-layout-fix-v2.js` поднят до версии `20260621-ux-2`;
- это нужно, чтобы браузер забрал новые модули, а не старый кеш.

### Edge Functions

- `leader-public-lead` — версия 6, публичные заявки с `request_id`, защитой от дублей и аудитом отправок.
- `leader-crm-leads` — версия 7, добавлено серверное действие `create_order_from_offer`.
- `leader-crm-orders` — версия 2, список и обновление заказов.

### Перенос CRM v4 в основной репозиторий

Начат перенос в каталог:

`crm/v4/`

Уже перенесены:

- `crm/v4/index.html`;
- базовые CSS и JS-файлы ядра;
- конфигурация CRM v4;
- helper-файлы API, UI, роутера и состояния.

Полный перенос модулей авторизации, заявок, потребностей, расчётов, КП и заказов нужно завершить отдельным безопасным шагом, чтобы не перенести неполные файлы и не нарушить работу текущей версии.

### Коммерческие предложения

Реализовано во временной CRM v4:

- формирование КП по сохранённому расчёту;
- короткий текст для мессенджеров;
- подробное предложение клиенту;
- копирование текстов;
- статусы КП;
- история событий;
- клиентский текст без внутренних финансовых данных;
- печатный/PDF-модуль КП переведён на встроенный HTML/CSS-логотип без внешней картинки.

### Создание заказа

Реализовано во временной CRM v4:

- создание заказа только из согласованного КП;
- перенос клиента, позиций и сумм;
- связь заявки, потребности, расчёта, КП и заказа;
- защита от повторного создания заказа;
- отображение созданного заказа в карточке заявки;
- серверное создание заказа из КП через `leader-crm-leads/create_order_from_offer`.

## Открытые задачи в GitHub Issues

- #2 — завершить перенос CRM v4 в основной репозиторий.
- #3 — аккуратно обновить версии CSS/JS на публичном сайте.
- #4 — провести полный ручной тест сайта и CRM v4.
- #5 — развить финансовый контур: оплаты, расходы, фактическая прибыль.
- #6 — добавить аудит публичных заявок и мягкий антиспам.
- #7 — добавить отображение аудита публичных заявок в CRM.

## Безопасность

Выполнено:

- усилена защита пользовательских ролей;
- обычный пользователь не может самостоятельно повысить себе доступ;
- владелец сохраняет полный контроль;
- администратор может управлять сотрудниками в рамках своих прав;
- закрыт лишний публичный доступ к созданию заказа;
- убрана старая чрезмерно широкая политика доступа к потребностям;
- начат отдельный контур улучшений после аудита сайта и CRM;
- усилена защита публичных заявок от дублей;
- уменьшен риск частично связанного заказа из КП;
- добавлен аудит публичных отправок формы сайта.

## Ближайшие задачи

1. Завершить полный перенос CRM v4 из `lidercalculator` в `lider-bsk/crm/v4/`.
2. Проверить полный сценарий с телефона: заявка → потребность → расчёт → КП → заказ.
3. Внедрить мобильный навигатор этапов.
4. Сделать отдельную карточку заказа v4.
5. Добавить производство, монтаж, оплаты, расходы и фактическую прибыль.
6. Вывести аудит публичных заявок в CRM.
7. Рассмотреть полноценную PostgreSQL RPC-транзакцию для создания заказа из КП, если инструмент позволит безопасно применить SQL-функцию.

## Ограничения

- CRM v4 пока не полностью перенесена в `lider-bsk`.
- Рабочая версия CRM v4 остаётся в `lidercalculator` до завершения проверки.
- Старую CRM v2 не удалять до завершения переноса и проверки.
- Таблицы `nav_*` не использовать для задач РА «Лидер».
- Удаления данных, таблиц, политик и функций выполнять только после отдельного подтверждения владельца.
