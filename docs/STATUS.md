# Статус проекта РА «Лидер»

Дата обновления: 2026-06-21.

## Основной контур

Основной репозиторий сайта и CRM:

`deputat36/lider-bsk`

Временная рабочая CRM v4:

`https://deputat36.github.io/lidercalculator/app-v4.html`

Основной публичный сайт:

`https://www.lider-bsk.ru`

Supabase project:

`ofewxuqfjhamgerwzull`

Для задач РА «Лидер» использовать только таблицы `leader_*`. Таблицы `nav_*` относятся к другому проекту и не используются.

## Публичный сайт

Выполнено:

- `robots.txt` существует и оформлен построчно;
- `sitemap.xml` существует и имеет XML-структуру;
- восстановлено видимое отображение логотипа в шапке сайта;
- добавлены CSS-ограничения от горизонтального переполнения;
- улучшена адаптация шапки, навигации, сеток и блоков под ноутбуки и планшеты;
- публичная форма передаёт `request_id`, `page_path`, `submitted_at` и технические данные источника;
- повторный клик по отправке блокируется на клиенте до завершения запроса.

## Supabase и публичные заявки

Проверено через Supabase:

- `leader-public-lead` активна, версия 6, публичная, без JWT;
- `leader-crm-leads` активна, версия 7, JWT включён;
- `leader-crm-orders` активна, версия 2, JWT включён.

Выполнено:

- в `leader_leads` добавлены `request_id`, `phone_normalized`, `source_page_path`, `submitted_at`, `client_user_agent`;
- добавлена уникальность `leader_leads.request_id`;
- добавлен индекс для поиска дублей по `phone_normalized` и дате создания;
- отправка заявки использует `on_conflict=request_id` и игнорирует повторную вставку дубля;
- создана таблица `leader_public_lead_audit`;
- RLS для `leader_public_lead_audit` включён;
- добавлены индексы аудита по дате, `request_id` и нормализованному телефону;
- чтение аудита доступно активным сотрудникам ролей `owner`, `admin`, `manager`;
- запись аудита разрешена публичному anon-контуру Edge Function;
- `leader-public-lead` пишет аудит для событий `accepted`, `suspicious`, `rejected`, `error`;
- ошибка записи аудита не блокирует получение основной заявки.

## CRM v4

Рабочий путь CRM v4 во временном репозитории:

1. Вход.
2. Список заявок.
3. Карточка заявки.
4. Потребности клиента.
5. Сохранённые расчёты.
6. Коммерческие предложения.
7. Создание заказа из согласованного КП.
8. Контроль заказов, производства, монтажа и финансов.
9. Управленческий дашборд.
10. Аудит публичных заявок.

## Перенос CRM v4 в основной репозиторий

Перенос начат в каталог:

`crm/v4/`

В основной репозиторий уже перенесены или добавлены:

- `crm/v4/assets/v4/config.js`;
- `crm/v4/assets/v4/api.js`;
- `crm/v4/assets/v4/state.js`;
- `crm/v4/assets/v4/ui.js`;
- `crm/v4/assets/v4/supabase-client.js`;
- `crm/v4/assets/v4/router.js`;
- `crm/v4/assets/v4/auth.js`;
- `crm/v4/assets/v4/auth-session-reset-v1.js`;
- `crm/v4/assets/v4/functions-client.js`;
- `crm/v4/assets/v4/crm-v4-tabs-lite.js`;
- `crm/v4/assets/v4/crm-diagnostics-v1.js`;
- `crm/v4/assets/v4/site-cache-note-v1.js`;
- `crm/v4/assets/v4/crm-ui-selfcheck-v1.js`;
- `crm/v4/assets/v4/public-lead-audit-v1.js`;
- `crm/v4/assets/v4/leads.js`;
- `crm/v4/assets/v4/lead-card.js`;
- `crm/v4/assets/v4/lead-create.js`;
- `crm/v4/assets/v4/lead-create.css`;
- `crm/v4/assets/v4/contact-control-v1.js`;
- `crm/v4/assets/v4/needs.js`;
- `crm/v4/assets/v4/calculations.js`.

Критичное исправление:

- `crm/v4/index.html` ссылался на `leads.js`, `lead-card.js`, `needs.js`, `calculations.js` и `supabase-client.js`, но этих файлов не было в `lider-bsk`;
- недостающие базовые файлы добавлены;
- `calculations.js` пока является безопасной заглушкой, чтобы карточка заявки не падала до переноса полноценного расчётного модуля.

`crm/v4/index.html` сейчас поддерживает:

- вход и выход;
- загрузку списка заявок;
- открытие карточки заявки;
- смену статуса заявки;
- сохранение следующего контакта;
- ручное создание заявки;
- добавление и архивирование потребностей;
- контроль контактов;
- аудит публичных заявок;
- диагностический блок CRM.

Важно:

- рабочая CRM v4 пока остаётся в `lidercalculator`;
- полноценный расчётный модуль ещё не перенесён;
- КП, заказы, финансы, производство, монтаж и дашборд ещё не перенесены в основной репозиторий полностью;
- крупные JS-модули лучше переносить логическими пакетами.

## Коммерческие предложения и заказы

Выполнено во временной CRM:

- КП создаётся из сохранённого расчёта;
- клиентский текст КП не раскрывает внутренние финансовые данные;
- печатный/PDF-модуль КП переведён на встроенный HTML/CSS-логотип без внешней картинки;
- заказ создаётся только из согласованного КП;
- переносится клиент, позиции и суммы;
- связываются заявка, потребность, расчёт, КП и заказ;
- есть защита от повторного создания заказа.

## Автоматические проверки

GitHub Actions проверяет:

- `robots.txt`;
- XML-структуру `sitemap.xml`;
- что URL из `sitemap.xml` ведут на существующие файлы;
- наличие `assets/public-lead-form.css` и `assets/public-lead-form.js`;
- что форма сайта содержит `request_id`, `page_path`, `submitted_at`;
- что CSS формы содержит защиту от горизонтального переполнения;
- что CSS формы повторно не скрывает логотип сайта;
- базовый поиск случайно закоммиченных секретных ключей.

## Документация

Ключевые документы:

- `AGENTS.md`;
- `docs/PROJECT.md`;
- `docs/STATUS.md`;
- `docs/DECISIONS.md`;
- `docs/AUDIT_IMPROVEMENTS.md`;
- `docs/LEADER_DB_HARDENING.md`;
- `docs/PUBLIC_LEAD_FORM_HARDENING.md`;
- `docs/PUBLIC_LEAD_AUDIT.md`;
- `docs/STATIC_CHECKS_RUNBOOK.md`;
- `docs/CRM_V4_TRANSFER_FILES.md`;
- `docs/NEXT_SAFE_STEPS.md`;
- `docs/MANUAL_TEST_CHECKLIST.md`;
- `crm/v4/TRANSFER.md`;
- `supabase/migrations/20260621_leader_public_lead_audit.sql`;
- `supabase/functions/leader-public-lead/index.ts`.

## Открытые задачи в GitHub Issues

- #2 — завершить перенос CRM v4 в основной репозиторий.
- #3 — аккуратно обновить версии CSS/JS на публичном сайте.
- #4 — провести полный ручной тест сайта и CRM v4.
- #5 — развить финансовый контур: оплаты, расходы, фактическая прибыль.
- #6 — добавить аудит публичных заявок и мягкий антиспам.
- #7 — добавить отображение аудита публичных заявок в CRM.

## Ближайшие задачи

1. Проверить в браузере перенесённый базовый контур CRM v4: вход, заявки, ручное создание заявки, карточку заявки, потребности, контроль контактов, аудит заявок, диагностику.
2. Проверить аудит публичных заявок на реальной отправке формы.
3. Перенести полноценный расчётный контур.
4. Перенести КП и создание заказа из КП.
5. Проверить полный сценарий: заявка → потребность → расчёт → КП → заказ → финансы → производство/монтаж.
6. Добавить оплаты, расходы и фактическую прибыль.
7. При необходимости добавить мягкий rate-limit публичной формы.

## Ограничения

- CRM v4 пока не полностью перенесена в `lider-bsk`.
- Рабочая версия CRM v4 остаётся в `lidercalculator` до завершения проверки.
- Старую CRM v2 не удалять до завершения переноса и проверки.
- Таблицы `nav_*` не использовать для задач РА «Лидер`.
- Удаления данных, таблиц, политик и функций выполнять только после отдельного подтверждения владельца.
