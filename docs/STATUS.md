# Статус проекта РА «Лидер»

Дата обновления: 2026-06-22.

## Основной контур

Основной репозиторий сайта и CRM:

`deputat36/lider-bsk`

Временная рабочая CRM v4:

`https://deputat36.github.io/lidercalculator/app-v4.html`

Основной публичный сайт:

`https://www.lider-bsk.ru`

Supabase project:

`ofewxuqfjhamgerwzull`

Для задач РА «Лидер» использовать только таблицы `leader_*`. Таблицы `nav_*` относятся к другому проекту и не используются.

## Supabase

Проверено через Supabase:

- `leader-public-lead` активна, версия 6, публичная, без JWT;
- `leader-crm-leads` активна, версия 7, JWT включён;
- `leader-crm-orders` активна, версия 2, JWT включён;
- в `leader-crm-leads` есть действие `create_order_from_offer`.

## Перенос CRM v4

Перенос идёт в каталог:

`crm/v4/`

В основной репозиторий уже перенесены или добавлены:

- базовые CSS и JS CRM v4;
- UI-полировка и адаптивные стили: `ui-polish.css`, `responsive-ui-v2.css`;
- панель контактов на сегодня: `followups.css`, `followups.js`;
- авторизация, состояние, роутер, UI-хелперы и Supabase-клиент;
- стабильное расширенное меню CRM: `crm-v4-expanded-menu-v1.js`;
- управленческий дашборд: `management-dashboard-v3.js`;
- список заявок, карточка заявки, ручное создание заявки;
- потребности клиента;
- контроль контактов;
- аудит публичных заявок;
- сохранённые, типовые и нестандартные расчёты;
- коммерческие предложения;
- карточка КП;
- печать/PDF КП с HTML/CSS-логотипом;
- создание заказа из согласованного КП через `leader-crm-leads`;
- блок связанного заказа в карточке заявки: `orders.css`, `orders.js`;
- отдельный быстрый раздел `Заказы`: `orders-fast-loader-v1.js`;
- базовая модальная карточка заказа: `order-card-v1.js`;
- контроль заказов: `order-control-v2.js`;
- финансовый контроль: `finance-control-v2.js`;
- доска производства и монтажа: `production-board-v3.js`;
- индикатор срочных производственных задач: `production-alerts-v1.js`;
- карточка производственного задания: `production-job-card-v2.js`;
- карточка монтажного задания: `installation-job-card-v2.js`.

`crm/v4/index.html` сейчас поддерживает:

- вход и выход;
- единое меню разделов: `Дашборд`, `Заявки`, `Заказы`, `Контроль заказов`, `Финансы`, `Производство`, `Контроль контактов`, `Аудит заявок`;
- более аккуратные статусы, карточки, кнопки, пустые состояния и адаптивные списки;
- панель `Кому связаться сегодня` в списке заявок;
- быстрый перенос следующего контакта на `+1 час` или `Завтра 10:00`;
- управленческий дашборд по заявкам, заказам, КП, финансам, производству и монтажу;
- загрузку списка заявок;
- открытие карточки заявки;
- возврат из карточки к списку;
- смену статуса заявки;
- сохранение следующего контакта;
- ручное создание заявки;
- добавление и архивирование потребностей;
- просмотр сохранённых расчётов и состава расчёта;
- создание типового расчёта;
- создание нестандартного расчёта;
- формирование КП из сохранённого расчёта;
- копирование короткого и полного текста КП;
- смену статуса КП;
- карточку КП;
- печать/PDF КП;
- создание заказа из согласованного КП;
- просмотр связанного заказа внутри карточки заявки;
- отдельный список заказов;
- открытие базовой карточки заказа из списка заказов;
- переход из заказа обратно к связанной заявке;
- контроль заказов по срокам, оплате, макетам и производству;
- финансовый контроль по оплатам, себестоимости, прибыли и марже;
- просмотр производства и монтажа;
- открытие, редактирование и печать производственного задания;
- открытие, редактирование и печать монтажного задания;
- контроль контактов;
- аудит публичных заявок;
- диагностический блок CRM.

Критичные исправления:

- `lead-card.js` исправлен: возврат из карточки к списку больше не должен запускать петлю route-change;
- базовый `calculations.js` остаётся в репозитории, но в `index.html` не подключается;
- `index.html` подключает дашборд, расширенное меню, UI-полировку, адаптивные стили, followups, расширенные расчёты, КП, связанный заказ, быстрый список заказов, карточку заказа, контроль заказов, финансовый контроль, производство и карточки производственных/монтажных заданий;
- `followups.js` использует существующие поля `leader_leads.next_contact_at` и не требует новой таблицы;
- `lead-timeline.js` из временной CRM пока не переносился, потому что зависит от `leader_lead_events`, которой не найдено в миграциях основного репозитория;
- `responsive-ui-v2.js` из временной CRM не переносился, потому что управляет другим набором вкладок (`clients`, `catalog`, `settings`) и может конфликтовать с текущим меню;
- `crm-v4-expanded-menu-v1.js` добавляет только реально перенесённые вкладки и выставляет единый порядок без дублей;
- `crm-ui-selfcheck-v1.js` теперь проверяет фактические перенесённые вкладки и показывает дубли кнопок меню;
- `management-dashboard-v3.js` адаптирован под уже перенесённые вкладки и не ссылается на неперенесённые `workdesk` и `production_control`;
- `orders.js` в основном репозитории использует серверное действие `create_order_from_offer`, а не старый клиентский сценарий `create_order` с ручным связыванием;
- `crm-v4-tabs-lite.js` знает вкладки `management_dashboard`, `orders`, `order_control`, `finance_control`, `production`, `contact_control` и `public_lead_audit` как управляемые разделы;
- `production-board-v3.js` показывает действия `Карточка`, `Печать`, `Открыть заказ` для производственных и монтажных заданий.

Проверено через GitHub connector:

- подключённые CSS/JS существуют в `crm/v4/assets/v4/`;
- `ui-polish.css`, `responsive-ui-v2.css`, `followups.css` и `followups.js` существуют и подключены в `index.html`;
- `crm-v4-expanded-menu-v1.js` существует и подключён в `index.html`;
- `management-dashboard-v3.js` существует и подключён в `index.html`;
- расчётные модули существуют и импортируют штатные `supabase-client`, `api`, `state`, `ui`;
- модули КП существуют и подключены в `index.html`;
- `orders.css` и `orders.js` существуют и подключены в `index.html`;
- `orders-fast-loader-v1.js`, `order-card-v1.js`, `order-control-v2.js`, `finance-control-v2.js`, `production-board-v3.js`, `production-alerts-v1.js`, `production-job-card-v2.js` и `installation-job-card-v2.js` существуют и подключены в `index.html`;
- `offer-order-create-v1.js` и `orders.js` используют `invokeLeaderFunction('leader-crm-leads', ...)` для серверного создания заказа.

Ограничение проверки:

- HTTP-запрос из текущего окружения к `https://deputat36.github.io/lider-bsk/crm/v4/` возвращает `403`;
- raw GitHub download из текущего окружения через терминал тоже возвращает `403`, поэтому локальный `node --check` через curl не выполнен;
- браузерная проверка в обычном браузере ещё нужна.

## Публичный сайт и аудит заявок

Выполнено:

- публичная форма передаёт `request_id`, `page_path`, `submitted_at` и технические данные источника;
- повторный клик по отправке блокируется на клиенте до завершения запроса;
- создана таблица `leader_public_lead_audit`;
- RLS для `leader_public_lead_audit` включён;
- чтение аудита доступно активным сотрудникам ролей `owner`, `admin`, `manager`;
- запись аудита разрешена публичному anon-контуру Edge Function;
- `leader-public-lead` пишет аудит для событий `accepted`, `suspicious`, `rejected`, `error`;
- ошибка записи аудита не блокирует получение основной заявки.

## Ещё нужно перенести

- при необходимости отдельный рабочий стол менеджера;
- история заявки после проверки или добавления таблицы `leader_lead_events`;
- дополнительные разделы временной CRM только после проверки, что они действительно нужны в основном контуре.

## Ближайшие задачи

1. Проверить в браузере перенесённый контур CRM v4: вход, меню, адаптивность, followups, дашборд, заявки, карточку заявки, потребности, расчёты, КП, создание заказа, связанный заказ, отдельный список заказов, карточку заказа, контроль заказов, финансы, производство, карточки производственных и монтажных заданий, контроль контактов, аудит заявок, диагностику.
2. Проверить аудит публичных заявок на реальной отправке формы.
3. Проверить полный сценарий: заявка → потребность → расчёт → КП → заказ → финансы → производство/монтаж.
4. При необходимости перенести отдельный рабочий стол менеджера.
5. Добавить оплаты, расходы и фактическую прибыль.
6. При необходимости добавить мягкий rate-limit публичной формы.

## Ограничения

- CRM v4 пока не полностью перенесена в `lider-bsk`.
- Рабочая версия CRM v4 остаётся в `lidercalculator` до завершения проверки.
- Старую CRM v2 не удалять до завершения переноса и проверки.
- Таблицы `nav_*` не использовать для задач РА «Лидер».
- Удаления данных, таблиц, политик и функций выполнять только после отдельного подтверждения владельца.