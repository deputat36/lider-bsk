# Публичный сайт РА «Лидер»: CSS страницы FAQ

Дата: 2026-07-13.

Контур: только публичный сайт.

## Цель

Перенести встроенные стили страницы `faq.html` в отдельный кешируемый файл без изменения клиентских ответов, FAQPage JSON-LD, SEO, формы или контактных данных.

## Выполнено

- создан `assets/public-faq.css?v=1`;
- удалён inline `<style>`;
- сохранены `assets/public-lead-form.css?v=6` и `assets/public-lead-form.js?v=7`;
- сохранены 12 видимых вопросов;
- сохранены 12 вопросов в FAQPage JSON-LD;
- сохранены canonical, robots, форма и номер телефона;
- внешний CSS подключён после общих стилей формы.

## Постоянная защита

Добавлены:

- `tools/check_public_faq_css.py`;
- `.github/workflows/public-faq-css-check.yml`.

Контракт проверяет:

- отсутствие inline `<style>`;
- единственное подключение нового CSS;
- порядок таблиц стилей;
- сохранение версий общей формы;
- количество видимых и JSON-LD вопросов;
- обязательные клиентские формулировки;
- ключевые CSS-селекторы и мобильный breakpoint;
- отсутствие удалённых CSS-ресурсов.

## Не изменено

- тексты ответов;
- структура FAQ;
- CRM UI;
- `nav_*` и `nav_v2_*`;
- Supabase schema, RLS, Auth, Edge Functions и production data.
