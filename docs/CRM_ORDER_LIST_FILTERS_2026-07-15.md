# Фильтры списка заказов CRM v4 — 2026-07-15

Список заказов получил поиск, операционные фильтры, сортировку, явный сброс и раздельные состояния ошибки, отсутствия данных и отсутствия совпадений.

Фильтры: активные, все, просроченные, оплата под контролем, проверить дизайн. Сортировка: новые, ближайший срок, сумма, статус.

Фильтр и сортировка сохраняются в `localStorage`. Поисковый текст не сохраняется, поскольку может содержать имя или телефон. Все операции выполняются над уже загруженными 40 заказами и не добавляют Network/Supabase вызовов.

Ручная проверка: ширины 360, 390, 768, 1024, 1366 и 1920 px; Tab/focus; восстановление фильтра после перезагрузки; отсутствие восстановления поиска; корректный сброс; три разных пустых/ошибочных состояния; отсутствие дополнительных запросов при фильтрации.

Production/staging Supabase, Auth, RLS, grants, policies, RPC, Edge Functions, schema и данные не изменялись.
