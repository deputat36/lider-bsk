# Следующие безопасные шаги по РА «Лидер»

Дата: 2026-06-24.

## Текущее состояние

- публичный сайт: `https://www.lider-bsk.ru`;
- страница проверки заявки: `https://www.lider-bsk.ru/request.html`;
- временная CRM v4: `https://deputat36.github.io/lidercalculator/app-v4.html`;
- основной CRM-контур: `https://deputat36.github.io/lider-bsk/crm/v4/`;
- основной репозиторий: `deputat36/lider-bsk`;
- Supabase project: `ofewxuqfjhamgerwzull`.

Использовать только объекты `leader_*`. `nav_*` относится к другому проектному контуру.

## Что уже сделано

1. Публичные заявки:
   - `leader-public-lead v7` активна;
   - работает защита от дублей по `request_id`;
   - аудит журналирует `leader_public_lead_audit_insert_failed` и `leader_public_lead_audit_request_failed` без блокировки заявки;
   - honeypot передаётся в Edge Function и фиксируется как `suspicious`;
   - страница `request.html` после успешной отправки показывает номер обращения, равный `request_id`.

2. Supabase CRM:
   - `leader-crm-leads v8` и `leader-crm-orders v2` активны с JWT;
   - прямой клиентский доступ к служебным RPC закрыт;
   - у `leader_*` нет отсутствующих индексов внешних ключей;
   - RLS чтения аудита оптимизирован через `(select auth.uid())` без изменения доступа;
   - live Supabase не менять без понятного плана, миграции и проверки.

3. Авторизация и контуры:
   - основной контур использует `leader_crm_v4_main_session`;
   - временный контур использует `leader_crm_v4_test_session`;
   - старый `leader_crm_v4_session` удаляется как устаревший общий ключ;
   - подключения авторизации обновлены до `20260624-contour-1`;
   - выход использует локальный scope;
   - массовое удаление `sb-*` и `supabase` ключей запрещено CI;
   - во временном клиенте обновление токена сериализовано через `refreshPromise`;
   - реализован `auth.getUser()` для диагностики.

4. CRM v4:
   - перенесены заявки, карточка, история, потребности, расчёты и КП;
   - работают создание заказа, список и карточка заказа;
   - подключены контроль заказов, финансы, производство, монтаж, контроль контактов, аудит и дашборд.

## Обязательная проверка изоляции

Из-за смены ключей потребуется один повторный вход в каждый контур.

1. Открыть основной контур и нажать Ctrl + F5.
2. Войти в основной контур.
3. Открыть временную CRM и нажать Ctrl + F5.
4. Отдельно войти во временную CRM.
5. Оставить обе вкладки открытыми не менее 2 минут.
6. Выйти из временной CRM.
7. Убедиться, что основная CRM осталась авторизована и загружает заявки.
8. Снова войти во временную CRM.
9. Выйти из основной CRM.
10. Убедиться, что временная CRM осталась авторизована.
11. Нажать `Проверить CRM` и проверить `Supabase Auth`, профиль и чтение таблиц.

## Проверка заявки и аудита

1. Открыть `https://www.lider-bsk.ru/request.html` и нажать Ctrl + F5.
2. Отправить заявку с пометкой `Тест CRM v4 audit v7`.
3. Записать номер обращения из зелёного сообщения об успешной отправке.
4. Открыть основной CRM-контур и раздел `Аудит заявок`.
5. Нажать `Обновить аудит`.
6. Найти событие по записанному номеру обращения (`request_id`).
7. Убедиться, что заявка с тем же `request_id` появилась в CRM.
8. Проверить телефон, страницу, UTM, user-agent, referer и технические данные.
9. Зафиксировать результат в `docs/CRM_V4_BROWSER_TEST_REPORT.md`.

## Полный бизнес-сценарий

После проверки авторизации пройти:

`заявка → потребность → расчёт → КП → согласование → заказ → финансы → производство → монтаж`.

## Следующие этапы

1. Исправить ошибки браузерной проверки.
2. Решить, нужен ли отдельный рабочий стол менеджера.
3. После подтверждения сценария добавить оплаты, расходы и фактическую прибыль.
4. Добавлять rate-limit только при подтверждённом спаме.

## Ограничения

- временную CRM не отключать до проверки;
- основной адрес не считать окончательно переключённым до полного сценария;
- старую CRM v2 не удалять без подтверждения;
- `nav_*` не использовать;
- POST-проверки из текущего окружения блокируются внешним фильтром;
- результат push-запусков GitHub Actions проверять в интерфейсе Actions.
