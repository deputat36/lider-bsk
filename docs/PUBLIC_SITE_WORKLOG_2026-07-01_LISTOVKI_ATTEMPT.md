# Flyers page attempt

Дата: 2026-07-01.

Проверен следующий шаг после страницы полиграфии: отдельная страница для листовок.

Итог:
- создание `listovki-borisoglebsk.html` несколько раз блокировалось фильтром инструмента;
- блокировка повторялась для полной версии с конструктором и для короткой версии без конструктора;
- текущая страница `poligrafiya-borisoglebsk.html` уже содержит листовки в перечне услуг и в тексте заявки;
- форма на странице полиграфии использует направление `Полиграфия` и не требует срочной правки.

Следующий безопасный вариант: вернуться к отдельной странице листовок позднее меньшим патчем или сделать ручной PR.

Supabase не менялся.
