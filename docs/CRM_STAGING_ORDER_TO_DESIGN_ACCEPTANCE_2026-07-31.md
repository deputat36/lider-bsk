# Staging acceptance «заказ → дизайн-задача» — 2026-07-31

## Фактический результат

В staging Supabase `otulfnouybahfnsycxqn` выполнен транзакционный сценарий:

`заказ → подтверждённая дизайн-потребность → дизайн-задача → событие`.

Runtime result:

`order-to-design acceptance: OK; cleanup verified: zero residue`.

## Что подтверждено

- активный manager с `design.write` может создать дизайн-задачу из заказа;
- заказ связан с заявкой и подтверждённой потребностью `need_design=true`;
- задача создаётся со статусом `Новая`;
- состояние макета устанавливается в `Макет не начат`;
- срок потребности переносится в задачу;
- одновременно создаётся одно событие `created`;
- повтор идентичной команды возвращает тот же результат с `idempotent_replay=true`;
- повтор того же idempotency key с изменённым payload возвращает `conflict`;
- вторая активная дизайн-задача по тому же заказу блокируется;
- созданная задача повторно читается.

## Очистка

Сценарий выполнен внутри `BEGIN`/`ROLLBACK` с префиксом
`LIDER-DESIGN-E2E-20260731`.

После rollback подтверждён нулевой остаток:

- профили — 0;
- заявки — 0;
- заказы — 0;
- потребности — 0;
- дизайн-задачи — 0;
- события — 0;
- command receipts — 0.

## Границы

Сценарий не содержит DDL, не меняет production, не развёртывает Edge Functions и
не затрагивает `nav_*` или `parket_*`.

Production Supabase не изменялся.
