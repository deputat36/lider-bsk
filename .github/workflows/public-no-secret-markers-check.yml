name: Public no secret markers check

on:
  push:
    branches: [main]
    paths:
      - '*.html'
      - 'assets/*.js'
      - 'assets/*.css'
      - '.github/workflows/public-no-secret-markers-check.yml'
  pull_request:
    branches: [main]
    paths:
      - '*.html'
      - 'assets/*.js'
      - 'assets/*.css'
      - '.github/workflows/public-no-secret-markers-check.yml'

jobs:
  public-no-secret-markers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check public files for secret markers
        shell: bash
        run: |
          set -euo pipefail
          files=(
            *.html
            assets/public-lead-form.js
            assets/public-lead-reference-v1.js
            assets/packages-link.js
            assets/public-lead-form.css
            assets/public-landing.css
          )
          markers=(
            'SUPABASE_SERVICE_ROLE'
            'service_role'
            'service-role'
            'sb_secret_'
            'JWT_SECRET'
          )
          for file in "${files[@]}"; do
            [ -f "$file" ] || continue
            for marker in "${markers[@]}"; do
              if grep -Fq "$marker" "$file"; then
                echo "Forbidden marker '$marker' found in $file" >&2
                exit 1
              fi
            done
          done
