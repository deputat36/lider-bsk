name: Patch profile-first browser async once

on:
  pull_request:
    branches:
      - main
    paths:
      - 'tools/patch_profile_first_browser_async_once.py'
      - 'tools/run-patch-profile-first-browser-async-once.txt'
      - '.github/workflows/patch-profile-first-browser-async-once.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'agent/profile-first-browser-check-v1'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Apply exact async patch
        run: |
          set -euo pipefail
          python3 tools/patch_profile_first_browser_async_once.py
          node --check tools/run_crm_profile_first_boot_browser_check.mjs
      - name: Commit fixed browser runner
        run: |
          set -euo pipefail
          if git diff --quiet -- tools/run_crm_profile_first_boot_browser_check.mjs; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tools/run_crm_profile_first_boot_browser_check.mjs
          git commit -m 'Не блокировать DOMContentLoaded в profile-first browser check'
          git push origin HEAD:${{ github.head_ref }}
